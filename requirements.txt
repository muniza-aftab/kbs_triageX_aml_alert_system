# Deployment requirements for the website only.
#
# The core package (src/triagex) has no runtime dependencies and this file does not change
# that: the inference engine, rule base, search and explanation facility all run on the
# standard library alone. What is listed here is the web layer, which is optional.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
